<%= form_with(model: ring) do |form| %>
  <% if ring.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ring.errors.count, "error") %> prohibited this ring from being saved:</h2>

      <ul>
        <% ring.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
